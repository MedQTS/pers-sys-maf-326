

## Fix navigation links

**Problem:** The nav link in `RunnerLayout.tsx` points to `/runner/week-v2`, but the route in `App.tsx` is `/runner/week`.

**Change:** In `src/components/RunnerLayout.tsx` line 6, change `/runner/week-v2` to `/runner/week`.

